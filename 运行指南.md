# basic_rq_daily 运行指南

本文说明如何在 **环境已就绪**（Python 依赖、米筐账号、`economic` 库、Mongo 可连）的前提下，用 **Python 命令** 完成日更与历史补齐。不依赖 `.bat` 脚本。

工作目录一律指 **`basic_rq_daily` 根目录**（含 `mongo_connect.py`、`trade_dates_all.csv` 的那一层）。

---

## 1. 前置条件（你已具备可跳过）

| 项 | 说明 |
|----|------|
| Python | 3.10+ |
| 依赖 | `pip install -r requirements-basic_rq_daily.txt` |
| 米筐 | 各脚本内 `rq.init(...)` 需为有效账号（外发前请改掉示例账号） |
| Mongo · `economic` | **已存在** `economic.trade_dates`（B 链路推导交易日用） |
| Mongo · `basic_rq` | 日更写入目标库，默认库名 `basic_rq` |
| 本地文件 | `trade_dates_all.csv` 与 A/SWL2 日更脚本同目录 |

### 1.1 连接配置（`mongo_connect.py`）

| 别名 | 用途 | 默认地址 |
|------|------|----------|
| `wonderwz27018_rw` | A 链路、SWL2、历史补齐 **写库** | `192.168.110.199:27018`（读写账号） |
| `local` | B 链路默认（读 `economic`、写 `basic_rq`） | 同上 host/port，**无认证** |

可用环境变量覆盖 `local` 的地址：`MONGO_HOST`、`MONGO_PORT`。

### 1.2 目标集合一览

| 集合 | 含义 |
|------|------|
| `rq_base_info` | 股票基础信息、`code_rq` |
| `rq_basic_financial` | 市值与部分财务 TTM |
| `rq_base_index` | 宽基成分 0/1 |
| `rq_daily_indusSWL2` | 申万二级行业成分 |
| `rq_daily_indusSWL2_price` | 申万二级成分 + 行业指数日 K |
| `rq_bench` | 基准指数行情 |
| `rq_quarterly` | 季报 PIT + universe 前向补齐 |
| `rq_yearly` | 年报 PIT + universe 前向补齐 |

---

## 2. 交易日规则（日更必读）

日更脚本默认 **T-1**：未传 `--date` 时，用 `trade_date_utils.previous_trade_date` 取 **严格早于今天** 的最近交易日（米筐当日数据通常晚间才稳定）。

- 建议 **每天 20:00 之后** 跑日更。
- 补跑指定日（与 T-1 无关）：

```bash
cd /d G:\github\packforcolleague\basic_rq_daily

python update_rqbaseInfo.py --date 20260515
# 亦支持：2026/05/15、2026-05-15
```

A 链路三步、SWL2 两步请对 **同一交易日** 依次执行（或全程使用相同 `--date`）。

---

## 3. 每日更新（推荐顺序）

### 3.1 A 链路（必须先按顺序跑）

```bash
cd /d G:\github\packforcolleague\basic_rq_daily

# 1) 基础信息
python update_rqbaseInfo.py

# 2) 基本财务（依赖同日 rq_base_info）
python update_rq_basic_financail.py

# 3) 宽基指数成分（依赖同日 rq_base_info）
python update_rq_in_index.py
```

说明：

- 任一步失败应停止，先排查再跑后续步骤。
- `update_rq_in_index.py` 的 `--date` 格式为 `YYYY-MM-DD`；另两个脚本支持 `YYYYMMDD` / `YYYY/MM/DD`。

### 3.2 SWL2 链路（建议在 A 之后）

```bash
cd /d G:\github\packforcolleague\basic_rq_daily

python update_rq_SWL2.py
python update_rq_SWL2_price.py
```

与 A 无硬依赖，但行业日更节奏建议与 A 同一天（同一 T-1）。

### 3.3 B 链路（季报 / 年报 / bench）

**前提**：对应 T-1 的 `rq_base_info` 已由 A 链路写入；`economic.trade_dates` 可用。

在 **`bench_quarterly_yearly` 目录** 下执行（脚本通过 `sys.path` 引用上级 `mongo_connect.py`）：

```bash
cd /d G:\github\packforcolleague\basic_rq_daily\bench_quarterly_yearly

# 1) 基准指数 bench
python -u MasterData/data_rq/update_daily_bench.py ^
  --mongo-alias local ^
  --mongo-db basic_rq ^
  --auto-mode previous_trade

# 2) 季报（含历史 backfill，耗时长）
python -u MasterData/data_rq/update_daily_rq_quarterly.py ^
  --mongo-alias local ^
  --mongo-db basic_rq ^
  --auto-mode previous_trade

# 3) 年报（含历史 backfill，耗时长）
python -u MasterData/data_rq/update_daily_rq_yearly.py ^
  --mongo-alias local ^
  --mongo-db basic_rq ^
  --auto-mode previous_trade
```

**仅做当日、不扫历史 backfill**（适合已补齐过历史的日常维护）：

```bash
python -u MasterData/data_rq/update_daily_rq_quarterly.py --mongo-alias local --mongo-db basic_rq --auto-mode previous_trade --no-backfill
python -u MasterData/data_rq/update_daily_rq_yearly.py --mongo-alias local --mongo-db basic_rq --auto-mode previous_trade --no-backfill
```

**显式指定交易日**（跳过自动推导）：

```bash
python -u MasterData/data_rq/update_daily_bench.py --pre-day 2026-05-20 --mongo-alias local --mongo-db basic_rq
```

**试跑不写库**：

```bash
python -u MasterData/data_rq/update_daily_bench.py --dry-run --mongo-alias local
```

B 链路常用参数：

| 参数 | 含义 |
|------|------|
| `--run-date` | 任务运行日，默认今天；与 `--auto-mode` 一起推导 `pre_trade_day` |
| `--auto-mode previous_trade` | T-1（默认） |
| `--auto-mode today_if_trade` | 运行日为交易日则更新当日 |
| `--pre-day` | 直接指定要更新的交易日，忽略 `--run-date` |
| `--no-backfill` | 季报/年报脚本：只做当日更新 |
| `--backfill-start` | backfill 起始日，默认 `2020-01-02` |
| `--mongo-alias` | 默认 `local` |
| `--mongo-db` | 默认 `basic_rq` |

### 3.4 一日总览（命令清单）

```text
20:00 后建议顺序：

1. cd basic_rq_daily
   python update_rqbaseInfo.py
   python update_rq_basic_financail.py
   python update_rq_in_index.py

2. python update_rq_SWL2.py
   python update_rq_SWL2_price.py

3. cd bench_quarterly_yearly
   python -u MasterData/data_rq/update_daily_bench.py --mongo-alias local --mongo-db basic_rq --auto-mode previous_trade
   python -u MasterData/data_rq/update_daily_rq_quarterly.py --mongo-alias local --mongo-db basic_rq --auto-mode previous_trade --no-backfill
   python -u MasterData/data_rq/update_daily_rq_yearly.py --mongo-alias local --mongo-db basic_rq --auto-mode previous_trade --no-backfill
```

首次部署或需要重扫历史时，去掉季报/年报命令上的 `--no-backfill`。

---

## 4. 历史数据补齐（一次性）

按顺序执行；**先改各脚本底部日期常量**（或 `__main__` 内查询区间），再运行。

| 顺序 | 命令 | 写入集合 | 需修改的位置 |
|------|------|----------|----------------|
| 1 | `python load_rqbaseInfofastmain.py` | `rq_base_info` | 文件末尾 `Client.economic.trade_dates.find(...)` 的 `$gte` / `$lte` |
| 2 | `python load_rq_basic_financialmain.py` | `rq_basic_financial` | `START_DATE`、`END_DATE`（或 `SINGLE_DAY`） |
| 3 | `python get_SWL2_2DB_Main.py` | `rq_daily_indusSWL2` | `date_range` 的 `$gte` / `$lte` |
| 4 | `python get_SWL2_2DB_price_Main.py` | `rq_daily_indusSWL2_price` | 同上 |
| 5 | `python get_rq_in_index.py` | `rq_base_index` | 顶部 `RANGE_START`、`RANGE_END`（须该区间已有 `rq_base_info`） |

示例（请先按实际区间改脚本内日期）：

```bash
cd /d G:\github\packforcolleague\basic_rq_daily

python load_rqbaseInfofastmain.py
python load_rq_basic_financialmain.py
python get_SWL2_2DB_Main.py
python get_SWL2_2DB_price_Main.py
python get_rq_in_index.py
```

`load_rq_basic_financialmain.py` 支持命令行（无需改文件即可试跑）：

```bash
python load_rq_basic_financialmain.py --mongo-alias wonderwz27018_rw
# 区间仍由文件内 START_DATE / END_DATE 控制
```

---

## 5. RQ 1 分钟线（独立分包）

分钟线不在本目录日更链内，见子目录 **`rq_minute_daily/运行说明.md`**（若存在）或 **`rq_minute_daily/README.md`**。  
Mongo 仍用上级 **`mongo_connect.py`**，打包时需与 `rq_minute_daily` 一并包含。

---

## 6. 常见问题

| 现象 | 处理 |
|------|------|
| `无法根据 run-date 解析 pre_trade_day` | 检查 `economic.trade_dates` 是否含目标日期前后记录 |
| `rq_base_info 中无数据` | 先跑 `update_rqbaseInfo.py`（或补齐历史步骤 1） |
| `不是交易日，跳过` | 非交易日跑日更时的正常提示；或检查 `trade_dates_all.csv` |
| `ModuleNotFoundError: dateutil` | `pip install python-dateutil` |
| `未知 mongo 别名` | 检查 `mongo_connect.py` 与脚本中的 `--mongo-alias` / 默认别名 |
| B 链路找不到 `mongo_connect` | 必须在 `bench_quarterly_yearly` 下执行，且上级目录存在 `mongo_connect.py` |
| Windows 控制台中文乱码 | 执行前 `chcp 65001`，或使用 UTF-8 终端 |

---

## 7. 与 README 的关系

- **`README.md`**：目录结构、打包说明、维护者同步。
- **本文**：面向运维的 **Python 运行步骤**（日更 + 历史 + B 链路参数）。

更细的脚本 docstring 见各 `.py` 文件头部说明。
