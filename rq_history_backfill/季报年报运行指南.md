按你给的区间 `2015-02-14` ~ `2026-01-01`，运行顺序如下（已写入 `rq_history_backfill/季报年报运行指南.md`）。

---

## 季报

**① 先拉米筐**

```bash
cd /home/alpha/basic_rq_daily

python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections quarterly \
  --start 2015-01-02 \
  --end 2015-01-26 \
  --no-ffill \
  --skip-existing \
  --mongo-alias wonderwz27018_rw
```

**② ① 跑完后 ffill**

```bash
python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections quarterly \
  --ffill-only \
  --start 2015-01-14 \
  --end 2026-05-28 \
  --mongo-alias wonderwz27018_rw
```

**③ 日常**

```bash
cd bench_quarterly_yearly
python -u MasterData/data_rq/update_daily_rq_quarterly.py \
  --mongo-alias wonderwz27018_rw --auto-mode previous_trade --no-backfill
```

---

## 年报

**① 先拉米筐**（把 `quarterly` 改成 `yearly`）

```bash
python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections yearly \
  --start 2015-02-14 \
  --end 2026-05-20 \
  --no-ffill \
  --skip-existing \
  --mongo-alias wonderwz27018_rw
```

**② ① 跑完后 ffill**

```bash
python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections yearly \
  --ffill-only \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --mongo-alias wonderwz27018_rw
```

**③ 日常**

```bash
python -u MasterData/data_rq/update_daily_rq_yearly.py \
  --mongo-alias wonderwz27018_rw --auto-mode previous_trade --no-backfill
```

---

季报和年报可分开跑；各自都是 **① → ② → ③**，② 不能跳过 ①。


<!-- # 季报 / 年报运行顺序

工作目录：`basic_rq_daily` 根目录。  
Mongo 写库：`--mongo-alias wonderwz27018_rw`（`192.168.110.199:27018` / `basic_rq`）。

**前提**：`rq_base_info` 在目标区间每日已有 `code_rq`。

**Linux 多行续写用 `\`**，不要用 PowerShell 的 `` ` ``。

---

## 季报（`rq_quarterly`）

### ① 先跑：逐日拉米筐（真实 PIT 快照）

```bash
cd /home/alpha/basic_rq_daily

python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections quarterly \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --no-ffill \
  --skip-existing \
  --mongo-alias wonderwz27018_rw
```

中断后续跑：保留 `--skip-existing`，会跳过已有 `date`。

---

### ② ① 完成后：全区间 ffill（按票对齐 universe）

**任选其一：**

```bash
# 方式 A：同一脚本
python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections quarterly \
  --ffill-only \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --mongo-alias wonderwz27018_rw
```

```bash
# 方式 B：不连米筐（推荐）
cd bench_quarterly_yearly

python -u MasterData/data_rq/ffill_rq_quarterly_yearly_to_universe.py \
  --collections quarterly \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --mongo-alias wonderwz27018_rw
```

---

### ③ 日常：每天 20:00 后（A 链路 `rq_base_info` 已更新）

```bash
cd /home/alpha/basic_rq_daily/bench_quarterly_yearly

python -u MasterData/data_rq/update_daily_rq_quarterly.py \
  --mongo-alias wonderwz27018_rw \
  --auto-mode previous_trade \
  --no-backfill
```

---

## 年报（`rq_yearly`）

与季报**互不依赖**，可单独、先后或并行执行。

### ① 先跑：逐日拉米筐

```bash
cd /home/alpha/basic_rq_daily

python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections yearly \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --no-ffill \
  --skip-existing \
  --mongo-alias wonderwz27018_rw
```

---

### ② ① 完成后：全区间 ffill

```bash
python -u rq_history_backfill/backfill_rq_quarterly_yearly.py \
  --collections yearly \
  --ffill-only \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --mongo-alias wonderwz27018_rw
```

或：

```bash
cd bench_quarterly_yearly

python -u MasterData/data_rq/ffill_rq_quarterly_yearly_to_universe.py \
  --collections yearly \
  --start 2015-02-14 \
  --end 2026-01-01 \
  --mongo-alias wonderwz27018_rw
```

---

### ③ 日常

```bash
cd /home/alpha/basic_rq_daily/bench_quarterly_yearly

python -u MasterData/data_rq/update_daily_rq_yearly.py \
  --mongo-alias wonderwz27018_rw \
  --auto-mode previous_trade \
  --no-backfill
```

---

## 一览

```text
季报：  ① 拉米筐 --collections quarterly --no-ffill
        ② ffill   --collections quarterly --ffill-only  （① 完成后）
        ③ 日常    update_daily_rq_quarterly.py

年报：  ① 拉米筐 --collections yearly --no-ffill
        ② ffill   --collections yearly --ffill-only    （① 完成后）
        ③ 日常    update_daily_rq_yearly.py
```

**注意**：② 必须在 ① 之后；② 只复制库里已有快照，不能代替 ① 拉米筐。 -->
