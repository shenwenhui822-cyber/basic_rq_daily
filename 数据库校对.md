# basic_rq 数据库校对任务

## 一、任务背景

同事已完成 RiceQuant（RQ）基础数据的落库，数据覆盖**历史全量**与**每日早盘增量更新**。本任务目标是系统性校对 `basic_rq` 库，确认落库数据在**完整性、准确性、时效性、一致性**上均可用于下游研究/生产。

| 项目 | 说明 |
|------|------|
| 数据源 | RiceQuant |
| 数据库 | MongoDB `192.168.110.199:27018` |
| 库名 | `basic_rq` |
| 数据形态 | 历史回填 + 每日早盘更新 |

---

## 二、校对范围

### 2.1 集合清单

| 集合 | 数据类型 | 更新频率 | 优先级 |
|------|----------|----------|--------|
| `rq_base_info` | 标的基础信息（代码、名称、上市日等） | 低频/事件驱动 | P0 |
| `rq_base_index` | 指数基础信息 | 低频 | P0 |
| `rq_daily_price_none` | 日线行情（不复权） | 每日早盘 | P0 |
| `rq_daily_indusSWL2` | 申万二级行业分类/归属 | 低频 + 变更日 | P1 |
| `rq_daily_indusSWL2_price` | 申万二级行业指数日线 | 每日早盘 | P1 |
| `rq_basic_financial` | 基本面财务数据 | 财报季/公告后 | P1 |
| `rq_quarterly` | 季度汇总数据 | 财报季 | P2 |
| `rq_yearly` | 年度汇总数据 | 年报季 | P2 |
| `rq_bench` | 基准/对照数据 | 视业务定义 | P2 |

> **P0 必须先完成**，P1/P2 可在 P0 通过后并行推进。

### 2.2 不在本次范围

- `admin` 库及系统库
- 落库脚本/调度逻辑本身的代码审查（除非校对发现系统性错误需回溯）

---

## 三、校对原则

1. **以 RQ 官方数据为准**（或团队约定的权威对照源），本地库为被检对象。
2. **先结构、后样本、再全量指标**：确认字段与索引无误后，再做抽样与统计校验。
3. **历史与增量分开验，但用同一套规则**：历史查覆盖与边界；增量查「最新交易日」是否及时、是否正确。
4. **可复现**：每条异常须记录查询条件、对照结果、截图/导出样例，便于开发修复与回归。
5. **先阻断 P0 问题**：缺日、错价、主键重复等直接影响使用的，优先上报。

---

## 四、通用校对项（所有集合适用）

### 4.1 元数据与结构

- [ ] 文档总量是否与预期量级一致（历史回填后应有明显基数；每日更新后应有合理增量）。
- [ ] 主键/唯一键组合是否无重复（常见：`order_book_id + date`、`order_book_id + report_date` 等，以实际字段为准）。
- [ ] 必填字段空值率：代码、日期、价格、行业代码等核心字段不应为空。
- [ ] 字段类型与取值范围：日期格式统一、价格为正数（除特殊字段）、枚举值在合法集合内。
- [ ] 索引是否满足查询与唯一性约束（Compass 或 `db.collection.getIndexes()` 查看）。

### 4.2 完整性

- [ ] **交易日覆盖**：按 A 股交易日历，检查是否存在缺失交易日、多余非交易日。
- [ ] **标的覆盖**：与 `rq_base_info` 中在册/应收录标的对比，是否存在应有无、不应有有。
- [ ] **时间边界**：最早日期、最晚日期是否符合回填任务书约定（如日线从 2010-01-01 起等）。

### 4.3 准确性（抽样 + 统计）

- [ ] **抽样比对**：每个 P0 集合至少抽 20 条（含边界：最新日、最早日、停牌、ST、退市整理等），与 RQ 官方接口或导出 CSV 逐字段对比。
- [ ] **统计校验**：同一日期下 `count`、`sum(volume)`、`avg(close)` 等与对照源差异是否在容忍阈值内（建议：价格类绝对误差 = 0 或 ≤ 1e-6；计数类 = 0）。
- [ ] **勾稽关系**：如 `high >= low`，`open/close` 在 `[low, high]` 内；复权与不复权关系（若后续有复权库可交叉验证）。

### 4.4 时效性（每日早盘更新）

- [ ] 最新一条数据的**业务日期**是否为「上一交易日」或任务约定的 `T-1`。
- [ ] 文档 `update_time` / `_id` 生成时间是否在**当日早盘更新窗口**内（需与落库同事确认窗口，如 08:00–09:30）。
- [ ] 连续 3 个交易日跟踪：更新是否稳定、无漏更、无重复写入。

### 4.5 一致性（跨集合）

- [ ] `rq_base_info` 中的 `order_book_id` 能否在 `rq_daily_price_none` 中找到对应行情（在上市期间）。
- [ ] 行业归属（`rq_daily_indusSWL2`）与行业指数（`rq_daily_indusSWL2_price`）代码体系一致。
- [ ] 财务数据报告期与 `rq_quarterly` / `rq_yearly` 汇总口径一致（抽样 5 家公司核对）。

---

## 五、分集合校对要点

### 5.1 `rq_base_info`（P0）

**关注点**：全市场标的主数据，下游依赖最多。

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 标的数量 | `count()` vs RQ 全 A/全市场列表 | 差异 ≤ 约定阈值（新股/退市当日除外） |
| 代码规范 | 抽样 50 条 | `order_book_id`、交易所后缀正确 |
| 上市/退市日 | 抽样 + 已知案例（如近期 IPO） | 与 RQ 一致 |
| ST/停牌状态字段 | 若有 | 与 RQ 当日状态一致 |
| 增量 | 对比昨日文档数 | 仅新增/变更应有增量，无异常 mass delete |

### 5.2 `rq_base_index`（P0）

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 指数列表完整 | 与 RQ 指数列表对比 | 主流宽基、行业指数无遗漏 |
| 指数代码与名称 | 抽样 20 条 | 一一对应无误 |
| 发布/基日等静态字段 | 抽样 | 与 RQ 一致 |

### 5.3 `rq_daily_price_none`（P0，核心）

**关注点**：日线不复权，使用频率最高。

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 最新交易日全市场覆盖 | 按 `date` 分组 `count` | 与当日应交易标的数一致（允许停牌无成交差异需说明） |
| 历史缺日 | 按标的聚合日期序列 vs 交易日历 | 无无故缺日 |
| OHLCV 准确 | 随机 30 条 × 3 个日期 | 与 RQ 完全一致 |
| 零成交/停牌 | 专门抽 10 条 | volume=0 或缺失规则符合约定 |
| 重复记录 | `order_book_id + date` 分组 | 无 count>1 |
| 每日增量 | 仅最新 `date` 文档 | 条数、更新时间符合预期 |

**建议 Mongo 自查示例**（字段名以实际为准）：

```javascript
// 最新交易日条数
db.rq_daily_price_none.aggregate([
  { $group: { _id: "$date", cnt: { $sum: 1 } } },
  { $sort: { _id: -1 } },
  { $limit: 5 }
])

// 重复键检查
db.rq_daily_price_none.aggregate([
  { $group: { _id: { id: "$order_book_id", d: "$date" }, cnt: { $sum: 1 } } },
  { $match: { cnt: { $gt: 1 } } },
  { $limit: 20 }
])
```

### 5.4 `rq_daily_indusSWL2`（P1）

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 行业代码版本 | 与 RQ 申万二级版本说明一致 | 2014/2021 等版本切换日若有需单独验证 |
| 标的行业归属 | 抽样 20 只股票 | 与 RQ 当日行业一致 |
| 变更历史 | 抽 3 只发生过行业调整的股票 | 调整生效日正确 |

### 5.5 `rq_daily_indusSWL2_price`（P1）

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 行业指数数量 × 日期 | 最新日 count | 与申万二级指数完整列表一致 |
| 价格准确 | 抽样 15 条 | 与 RQ 一致 |
| 与分类表一致 | 行业代码集合对比 | 两表代码体系无孤儿/缺失 |

### 5.6 `rq_basic_financial`（P1）

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 报告期覆盖 | 按 `report_date` 统计 | 近年季报/年报期齐全 |
| 字段准确 | 抽 5 家公司 × 2 个报告期 | 营收、净利润等核心字段与 RQ 一致 |
| 公告滞后 | 最新财报季 | 落库时间不早于 RQ 可用时间（允许约定 T+N） |

### 5.7 `rq_quarterly` / `rq_yearly`（P2）

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 汇总口径 | 与 `rq_basic_financial` 抽样勾稽 | 同一公司同一报告期关键指标一致 |
| 时间序列连续 | 按公司看季度/年度序列 | 无跳期（除非公司本身无披露） |

### 5.8 `rq_bench`（P2）

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 基准定义 | 与需求文档/落库说明对齐 | 基准代码、权重、成分规则明确 |
| 数值准确 | 抽样 | 与 RQ 或约定对照源一致 |

---

## 六、执行计划（建议 5 个工作日）

| 阶段 | 时间 | 内容 | 产出 |
|------|------|------|------|
| D1 | 第 1 天 | 环境打通；导出各集合样例；确认字段字典与主键；跑通用结构/重复/空值检查 | 《结构检查记录》 |
| D2 | 第 2 天 | P0：`rq_base_info`、`rq_base_index` 全量指标 + 抽样 | P0 基础信息报告 |
| D3 | 第 3 天 | P0：`rq_daily_price_none` 历史缺日 + 最新 5 日全量统计 + 抽样比对 | 日线校对报告 |
| D4 | 第 4 天 | P1：行业、财务；跨集合一致性 | P1 报告 |
| D5 | 第 5 天 | P2 + 连续 3 日增量跟踪；汇总问题单；回归已修复项 | 《校对总结与问题清单》 |

> 若人力有限：**至少完成 D1–D3**，其余可列为第二阶段。

---

## 七、对照源与工具

| 用途 | 建议 |
|------|------|
| 权威对照 | RQ 官方 API / RQData 终端导出（与落库同源） |
| 辅助对照 | 交易所公告、Wind/同花顺（仅用于争议字段二次确认） |
| 连接 | MongoDB Compass 或 `mongosh` 连接 `192.168.110.199:27018` |
| 交易日历 | 团队统一 A 股交易日历文件（避免各用各的） |
| 记录 | Excel/飞书表格：问题编号、集合、查询条件、期望/实际、严重程度 |

---

## 八、问题分级与上报

| 级别 | 定义 | 示例 | 处理时效 |
|------|------|------|----------|
| **Blocker** | 不可用或大面积错误 | 最新交易日缺 30%+ 标的；价格系统性偏差 | 当天上报，修复后再验 |
| **Major** | 部分场景错误 | 某行业指数一段日期缺失；财务某字段全空 | 1 个工作日内 |
| **Minor** | 边界/个别标的 | 单只股票某天 volume 差 1；名称更新延迟 1 日 | 记录，批量修复 |

**上报内容模板**：

```
问题编号：RQ-CHK-001
集合：rq_daily_price_none
严重程度：Blocker / Major / Minor
发现方式：统计 / 抽样 / 增量跟踪
查询条件：{ date: "2025-06-02", order_book_id: "000001.XSHE" }
期望值：（对照源截图或数值）
实际值：（库内截图或数值）
影响范围：约 xx 条 / xx 标的 / xx 日期
建议：补数 / 重跑某日任务 / 修正字段映射
```

---

## 九、验收标准（任务关闭条件）

- [ ] P0 集合：无 Blocker；Major ≤ 3 且均有修复计划或业务豁免说明。
- [ ] 最新一个交易日 P0 数据：**覆盖率、准确率、更新时效**三项均通过。
- [ ] 历史数据：缺日率、重复率、空值率在团队约定阈值内（建议：重复率=0，核心字段空值率<0.1%）。
- [ ] 跨集合一致性抽检通过（至少 10 组勾稽无 Major 及以上问题）。
- [ ] 《校对总结与问题清单》已提交，Blocker/Major 已指派跟进人。

---

## 十、附录：每日早盘增量核对清单（可打印）

**日期：__________　核对人：__________**

1. [ ] 确认「上一交易日」日期：__________
2. [ ] `rq_daily_price_none` 该日文档数：______（预期约 ______）
3. [ ] 随机 5 条价格与 RQ 一致
4. [ ] `rq_daily_indusSWL2_price` 该日文档数：______
5. [ ] `rq_base_info` 是否有异常大量变更
6. [ ] 更新任务完成时间：______（是否在约定窗口内）
7. [ ] 异常记录编号：______

---

## 十一、需落库同事提前提供的信息

校对开始前，请落库同事补充以下材料（可附在本文档后）：

1. 各集合**字段说明**与**唯一键定义**
2. 历史回填的**时间范围**与**标的范围**
3. 每日早盘任务的**调度时间、cron、依赖顺序**
4. RQ 接口版本、复权口径说明（`none` 已明确为不复权）
5. 已知问题/豁免清单（若有）

---

*文档版本：v1.0 | 适用库：basic_rq @ 192.168.110.199:27018*
