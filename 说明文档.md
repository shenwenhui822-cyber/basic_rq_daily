连接地址：192.168.110.199:27018  
只读用户名：reader  
只读密码：readonly_wonderwz  
库：basic_rq  

---

## rq_base_info 表：全市场标的基础信息

**已存在数据**：2015/01/05 ~ 至今  
**定时导入**：每交易日 08:03 更新**前一交易日（T-1）**数据  
**写入方式**：按 `date` 先删后插；同一交易日仅保留一批全市场截面  

```json
{
  "_id": { "$oid": "6a0ff3b6834f953ceb166cb3" },  // MongoDB 文档主键，系统自动生成，非米筐字段
  "date": "2015-01-05",  // 数据截面交易日，统一为 YYYY-MM-DD 字符串；与 economic.trade_dates 对齐
  "code": "SZ000002",  // 内部展示代码：深市 SZ+6位、沪市 SH+6位，由 code_rq 转换而来
  "code_rq": "000002.XSHE",  // 米筐 order_book_id，拉数与关联的主键之一
  "trade_status": 1,  // 当日是否可交易：1=未停牌可交易，0=停牌；来自 is_suspended，整数 0/1，不做小数处理
  "riskwarning": 0 , // 是否 ST/*ST 风险警示：1=是，0=否；来自 is_st_stock，整数 0/1，不做小数处理
  "list_days": 31   // 上市至该日天数，整数
}
```

**字段处理摘要**

| 字段 | 含义 | 入库处理 |
|------|------|----------|
| date | 截面交易日 | 统一 YYYY-MM-DD |
| code | SH/SZ 前缀代码 | 由 code_rq 规则转换 |
| code_rq | 米筐合约 | 原样 |
| trade_status | 停牌标记 | 0/1 整数 |
| riskwarning | ST 标记 | 0/1 整数 |
| list_days | 上市天数 | 整数，异常时为 null |

---

## rq_daily_price_none 表：全市场不复权日线

**已存在数据**：2020/01/02 ~ 至今  
**定时导入**：每交易日 08:20 更新 T-1  
**依赖**：须先有同日 `rq_base_info` 中的 `code_rq` 列表  
**写入方式**：按 `date` 先删后插  

```json
{
  "_id": { "$oid": "6a15a13a7ae338c3d4ea9396" },  // MongoDB 主键
  "date": "2020-01-02",  // 交易日期，YYYY-MM-DD；入库前统一为横线格式
  "code": "SH600000",  // 展示代码 SH/SZ+6位
  "code_rq": "600000.XSHG",  // 米筐合约
  "open": 12.47,  // 不复权开盘价（元）；四舍五入保留 2 位小数
  "high": 12.64,  // 不复权最高价（元）；2 位小数
  "low": 12.45,  // 不复权最低价（元）；2 位小数
  "close": 12.47,  // 不复权收盘价（元）；2 位小数
  "prev_close": 12.37,  // 前收（元）；2 位小数
  "volume": 51629079,  // 成交量（股）；2 位小数（整股数通常仍为整数）
  "total_turnover": 647446166,  // 成交额（元）；2 位小数
  "limit_up": 13.61,  // 涨停价（元）；2 位小数
  "limit_down": 11.13  // 跌停价（元）；2 位小数
}
```

**数值字段统一规则**：`open/high/low/close/prev_close/volume/total_turnover/limit_up/limit_down` 均用**四舍五入、保留 2 位小数**；缺失为 `null`。

---

## rq_basic_financial 表：全市场基本财务截面

**已存在数据**：2015/01/05 ~ 至今  
**定时导入**：每交易日 08:05 更新 T-1  
**含义**：每个交易日一条全市场财务快照（非财报发布日专用表）  

```json
{
  "_id": { "$oid": "6a13b667a4eeb309000c4a4f" },  // MongoDB 主键
  "date": "2015-01-05",  // 截面交易日，YYYY-MM-DD
  "code": "SH600000",  // 展示代码
  "code_rq": "600000.XSHG",  // 米筐合约
  "mkt_cap_ard": 299761285639.05,  // 总市值（元，ARD 口径）；2 位小数
  "total_shares": 18653471415,  // 总股本（股）；2 位小数
  "free_float_shares": 9948132145,  // 自由流通股本（股）；2 位小数
  "or_ttm": 117311000000,  // 营业收入 TTM（元）；2 位小数
  "gr_ttm": 117311000000,  // 营业总收入 TTM（元）；2 位小数
  "netprofit_ttm": 45903000000,  // 净利润 TTM（元）；2 位小数
  "operatecashflow_ttm": 86043000000  // 经营活动现金流 TTM（元）；2 位小数
}
```

**数值字段统一规则**：上述 7 个财务数值字段均**四舍五入保留 2 位小数**；NaN 入库为 `null`。

---

## rq_base_index 表：股票指数成分归属

**已存在数据**：2015/01/05 ~ 至今  
**定时导入**：每交易日 08:07 更新 T-1  
**依赖**：须先有同日 `rq_base_info`  
**含义**：标记每只股票是否属于上证50/沪深300/中证500/1000/2000 成分，非指数行情本身  

```json
{
  "_id": { "$oid": "6a13ac51d167acd648c43ab2" },  // MongoDB 主键
  "date": "2026-01-05",  // 截面交易日，YYYY-MM-DD
  "code": "SZ000001",  // 展示代码
  "code_rq": "000001.XSHE",  // 米筐合约；与 rq_base_info 当日列表对齐
  "in_SZ50": 0,  // 是否上证 50 成分：1=是，0=否；来自 index_components(000016.XSHG)，int32
  "in_HS300": 1,  // 是否沪深 300 成分：000300.XSHG
  "in_ZZ500": 0,  // 是否中证 500 成分：000905.XSHG
  "in_ZZ1000": 0,  // 是否中证 1000 成分：000852.XSHG
  "in_ZZ2000": 0  // 是否中证 2000 成分：932000 系列指数，int32；仅 0/1，无小数处理
}
```

**处理说明**：五个 `in_*` 字段由米筐 `index_components` 与当日 `code_rq` 做集合包含判断，存 **0/1 整数**，不做四舍五入。

---

## rq_bench 表：基准指数日行情

**已存在数据**：2020-01-02 ~ 至今  
**定时导入**：每交易日 08:07 更新 T-1（与宽基成分任务同时段，表独立）  

**六个 Wind 代码与米筐合约对应关系**

| code（落库） | code_rq（拉数） | 说明 |
|--------------|-----------------|------|
| 000001.SH | 000001.XSHG | 上证综指 |
| 399001.SZ | 399001.XSHE | 深证成指 |
| 881001.WI | 399317.XSHE | 万得全 A；米筐无 881001，用 399317 替代拉数 |
| 000300.SH | 000300.XSHG | 沪深 300 |
| 000905.SH | 000905.XSHG | 中证 500 |
| 000852.SH | 000852.XSHG | 中证 1000 |

```json
{
  "_id": { "$oid": "6a168374e16a4f329f24c3d8" },  // MongoDB 主键
  "date": "2020-01-02",  // 交易日期，YYYY-MM-DD
  "code": "000001.SH",  // Wind 风格指数代码（落库标识，不因替代拉数而改变）
  "code_rq": "000001.XSHG",  // 实际用于 get_price 的米筐合约
  "rq_index_code": "000001",  // 从 code_rq 截取的点号前数字部分，便于展示
  "rq_bench_substitute": false,  // 是否使用了替代指数拉数：881001.WI→399317 时为 true
  "pct_chg": "0.01149907348029",  // 涨跌幅比例（非百分号）：(close/pre_close)-1；旧数据可能为长字符串
  "volume": 29247.0208,  // 成交量，单位：百万股；米筐 volume÷1,000,000；2 位小数
  "amt": 327197122605.8,  // 成交额（元），来自 total_turnover；2 位小数
  "pre_close": 3050.124000000001,  // 前一交易日收盘价（元）；2 位小数
  "close": 3085.1976,  // 当日收盘价（元）；2 位小数
  "open": 3066.3357,  // 开盘价（元）；2 位小数
  "high": 3098.1001,  // 最高价（元）；2 位小数
  "low": 3066.3357  // 最低价（元）；2 位小数
}
```

**数值处理规则（新入库及历史规范化后）**

| 字段 | 精度 | 其它处理 |
|------|------|----------|
| open/high/low/close/pre_close | 2 位小数，四舍五入 | 不复权日线 OHLC |
| volume | 2 位小数 | 百万股 |
| amt | 2 位小数 | 元 |
| pct_chg | **4 位小数**，四舍五入 | 比例值，0.0115 表示约 1.15%；入库为 **float** |

**历史数据说明**：2026 年前部分文档 `pct_chg` 仍为 14 位字符串；可按区间批量规范化为 **float 四位**。`pre_close` 与 `pct_chg` 由近两日 close 序列计算，非米筐直接字段。

---

## rq_daily_indusSWL2 表：申万二级行业成分

**已存在数据**：2020/01/02 ~ 至今  
**定时导入**：每交易日 08:10 更新 T-1  

```json
{
  "_id": { "$oid": "6a152e089f238fde827906b2" },  // MongoDB 主键
  "date": "2020-01-02",  // 截面交易日，YYYY-MM-DD
  "indus_code": "801011",  // 申万二级行业代码（801xxx）
  "name": "林业",  // 申万二级行业名称
  "stocks": "['000592.XSHE', '002679.XSHE', '600265.XSHG']"  // 该行业成分股 code_rq 列表，以字符串形式存储（非 BSON 数组）
}
```

**处理说明**：来自米筐 `get_industry_mapping(source='sws')`；`stocks` 为 Python 列表转字符串入库，使用时需解析；无数值四舍五入。

---

## rq_daily_indusSWL2_price 表：申万二级行业指数日线

**已存在数据**：2020/01/02 ~ 至今  
**定时导入**：每交易日 08:13 更新 T-1  
**与 rq_daily_indusSWL2 关系**：同 `date + indus_code` 对应，本表补充行业指数 OHLCV  

```json
{
  "_id": { "$oid": "6a1531fdde1f63c0990cc520" },  // MongoDB 主键
  "date": "2020-01-02",  // 交易日期，YYYY-MM-DD
  "indus_code": "801011",  // 申万二级行业代码
  "name": "林业",  // 行业名称（与成分表一致）
  "stocks": "['000592.XSHE', '002679.XSHE', '600265.XSHG']",  // 成分股列表字符串，与 SWL2 表同步
  "open": 1170.09,  // 行业指数开盘；来自 get_price，当前未单独做 2 位强制格式化
  "high": 1177.72,  // 最高价
  "low": 1158.62,  // 最低价
  "close": 1164.143,  // 收盘价
  "volume": 32492213,  // 成分股成交量合计（股）
  "total_turnover": 123632906  // 成分股成交额合计（元）
}
```

**处理说明**：价量字段为米筐原始精度入库；若需与 `rq_daily_price_none` 一致的 2 位规则，需另行批量规范化。

---

## rq_quarterly 表：季报 PIT 财务截面

**已存在数据**：2015/01/15 ~ 至今  
**定时导入**：每交易日 08:20 更新 T-1  
**含义**：Point-in-Time 季报字段——每个交易日可见的**最新已披露季报**快照  

```json
{
  "_id": { "$oid": "6a1924ae7fa7ec551e49016d" },  // MongoDB 主键
  "date": "2015-01-15",  // 观测日（PIT 截面日），YYYY-MM-DD；非报告期本身
  "code": "SZ002107",  // 展示代码
  "code_w": "002107.SZ",  // Wind 风格代码（.SZ/.SH），便于与 Wind 对照
  "code_rq": "002107.XSHE",  // 米筐合约；与 rq_base_info 对齐
  "rptdate": "2014-12-31",  // 报告期截止日（如 2014 年报对应 2014-12-31）
  "stm_issuingdate": "2015-01-15",  // 该报告在 PIT 口径下的披露/生效日
  "monetary_cap": 336023980.82,  // 货币资金（元）
  "minority_int": 0,  // 少数股东权益（元）
  "tot_equity": 631744756.72,  // 股东权益合计（元）
  "tot_assets": 668638484.35,  // 资产总计（元）
  "tot_liab": 36893727.63,  // 负债合计（元）
  "interestexpense_ttm": null,  // 利息支出 TTM；无数据时为 null
  "gc_ttm2": 286661881.88,  // 营业总成本 TTM（gc_ttm2 口径）
  "or_ttm2": 315307987.25,  // 营业收入 TTM（or_ttm2 口径）
  "netprofit_ttm2": 36547999.82,  // 净利润 TTM（netprofit_ttm2 口径）
  "ebit2_ttm": 27931307.04  // EBIT TTM（ebit2 口径）
}
```

**处理说明**

- 主流程：米筐 PIT 接口按 `date` 拉取，写入前按 `(date, code)` 去重索引。   
- 数值字段：米筐原值入库，**未统一 2 位小数**（与 `rq_basic_financial` 不同）。

---

## rq_yearly 表：年报 PIT 附注截面

**已存在数据**：2015/01/15 ~ 至今  
**定时导入**：每交易日 08:25 更新 T-1  

```json
{
  "_id": { "$oid": "6a18f1c6a9a6dfc2b639610c" },  // MongoDB 主键
  "date": "2015-01-26",  // 观测日（PIT 截面日），YYYY-MM-DD
  "code": "SZ002107",  // 展示代码
  "code_w": "002107.SZ",  // Wind 风格代码
  "code_rq": "002107.XSHE",  // 米筐合约
  "rptdate": "2014-12-31",  // 报告期截止日
  "stm_issuingdate": "2015-01-15",  // 报告披露/生效日（PIT）
  "stmnote_tax": 0.00003283245613884414,  // 有效税率等年报附注指标（比例/比率），米筐原精度
  "stmnote_audit_category": 1  // 审计意见类别编码（整数枚举）；1 等含义以米筐字段定义为准
}
```

**处理说明**：与季报相同；附注类字段保持米筐原始精度，无统一四舍五入。

---

## rq_minute 库 · rq_minute_none_YYYY 表：全市场 1 分钟线

**库名**：`rq_minute`（与 `basic_rq` 不同库）  
**表名**：按数据日**年份**分表，如 `rq_minute_none_2026`  
**已存在数据**：2026/01/05 ~ 至今（随年份递增新建 `rq_minute_none_YYYY`）  
**定时导入**：每交易日 08:35 更新 T-1  
**依赖**：须先有 T-1 的 `rq_base_info`  
**写入方式**：按 `date` 先删后插；索引 `(date, time, code_rq)` 唯一  

```json
{
  "_id": { "$oid": "6a1f9d9c7666c450fa9817fa" },  // MongoDB 主键
  "date": "2026-01-05",  // 交易日期，YYYY-MM-DD
  "time": "15:00:00",  // 分钟时刻，HH:MM:SS（如 09:31:00 … 15:00:00）
  "code": "SH688775",  // 展示代码
  "code_rq": "688775.XSHG",  // 米筐合约
  "open": 245.55,  // 该分钟开盘价（不复权）；米筐原值，未强制 2 位
  "high": 245.55,  // 该分钟最高价
  "low": 245.55,  // 该分钟最低价
  "close": 245.55,  // 该分钟收盘价
  "volume": 25702,  // 该分钟成交量（股）
  "total_turnover": 6311126  // 该分钟成交额（元）
}
```

**处理说明**

- 全市场按 `code_rq` 分批拉 1m（open/high/low/close/volume/total_turnover）。  
- 价量**未**做与日线相同的 2 位四舍五入，保持米筐返回精度。  

---

## 跨表通用约定

| 项目 | 约定 |
|------|------|
| date | 业务日期统一 **YYYY-MM-DD** 字符串 |
| code / code_rq | 展示码与米筐码成对出现；分钟/日线/财务均依赖 base_info 的 code_rq 数据 |
| 去重写入 | 日线、分钟、bench 等按日任务多为**删当日再 insert** |
| null | 米筐缺失或 NaN 入库为 `null`，非 0 |
| 四舍五入 | **2 位**：`rq_daily_price_none`、`rq_basic_financial`、`rq_bench` 价量；**4 位**：`rq_bench.pct_chg` |
